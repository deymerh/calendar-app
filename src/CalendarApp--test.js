import { render, screen } from '@testing-library/react';
import { CalendarApp } from './CalendarApp';

//describe('Prubas en el componente Principal <CalendarApp />', () => {
  // test('renders Ingreso', () => {
  //   render(<CalendarApp />);
  //   const linkElement = screen.getByText(/Ingreso/i);
  //   expect(linkElement).toBeInTheDocument();
  //});
//});
